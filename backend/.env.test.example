# Test database — separate from dev to prevent data loss
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/issue_tracker_test?schema=public"

# JWT defaults for tests
JWT_EXPIRY=1d
JWT_COOKIE_MAX_AGE=86400000